declare const Parser: {
    parse: typeof parse;
    parseOne: typeof parseOne;
};
interface ParseOneResult {
    length: number;
    value: any;
}
declare function parseOne(file: string): ParseOneResult;
declare function parse(file: string): any;

declare const Serializer: {
    serialize: typeof serialize;
};
type FormatObj = {
    mode: 'line';
} | {
    mode: 'compact';
} | {
    mode: 'indent';
    space?: number;
} | {
    mode: 'pretty';
    threshold?: number;
    space?: number;
};
type Format = 'line' | 'compact' | 'pretty' | 'indent' | number | FormatObj;
declare function serialize(obj: unknown, format?: Format): string;

export { type Format, type FormatObj, Parser, Serializer };
